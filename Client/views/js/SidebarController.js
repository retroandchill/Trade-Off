/**
 * Handles all the scripts related to the sidebar
*/
var $ = require('jQuery');

/**
 * Toggles the sidebar's visibility
 */
function toggleSidebar() {
    console.log("It's not working!");
    $('sidebarmenu').sidebar('toggle');
}